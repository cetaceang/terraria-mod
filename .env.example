# Server runtime config is file-based now:
#   ./config/serverconfig.txt
# Do NOT put SERVER_PORT / MAX_PLAYERS / WORLD_NAME here.

# Optional container timezone override
TZ=Asia/Shanghai

# Auto update check on container start.
# Downloads only when release version changes.
AUTO_UPDATE_ON_START=true

# Auto mod update check on container start.
# Downloads only changed mods when Workshop update is detected.
AUTO_DOWNLOAD_MODS=true

# Comma-separated Steam Workshop IDs (used when AUTO_DOWNLOAD_MODS=true)
MOD_IDS=2824688072,2824688266,2825151264,2563309347,2908170107,2599842771,2797518634,2619954303,2669644269

# Optional override for tModLoader release zip URL.
# Default behavior already uses:
# https://github.com/tModLoader/tModLoader/releases/latest/download/tModLoader.zip
# Set this only when you want to pin a specific version.
# TML_RELEASE_URL=https://github.com/tModLoader/tModLoader/releases/download/v2025.03.3.1/tModLoader.zip

# Put local .tmod files under ./data/mods on host.
# They are still loaded and included in enabled.json.
# Set AUTO_DOWNLOAD_MODS=false to disable Workshop downloads and use local mods only.

# Remove old .tmod files not included in current desired set
CLEAN_OLD_MODS=true
